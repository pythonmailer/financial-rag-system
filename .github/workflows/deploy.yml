name: Financial RAG CI/CD Pipeline

# Trigger this workflow every time you push to the 'main' branch
on:
  push:
    branches: [ "main" ]

jobs:
  # --- STAGE 1: Continuous Integration (Testing) ---
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Pytest
        env:
          # Bypasses heavy model loading and real DB connections
          TESTING: "True"
          DATABASE_URL: "sqlite:///./test_database.db"
          
          # Satisfies OpenAI client validation at import time
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: pytest tests.py
        
  # --- STAGE 2: Continuous Deployment (AWS EC2) ---
  deploy:
    needs: test # Ensures deployment ONLY happens if tests pass!
    runs-on: ubuntu-latest
    steps:
      - name: SSH into AWS and Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          # Injects secrets into the shell environment of the remote server
          envs: GROQ_API_KEY, GEMINI_API_KEY, OPENROUTER_API_KEY, DATABASE_URL
          script: |
            cd ~/financial-rag-system
            
            echo "GROQ_API_KEY=$GROQ_API_KEY" > .env
            echo "GEMINI_API_KEY=$GEMINI_API_KEY" >> .env
            echo "OPENROUTER_API_KEY=$OPENROUTER_API_KEY" >> .env
            echo "DATABASE_URL=$DATABASE_URL" >> .env
            echo "TESTING=False" >> .env

            git pull origin main
            sudo docker system prune -f 
            sudo docker-compose up -d --build
        env:
          # Maps GitHub Secrets to the variables used in the 'envs' field above
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          DATABASE_URL: "postgresql://admin:adminpassword@postgres:5432/financial_rag"